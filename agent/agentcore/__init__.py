"""A local, self-improving AI agent.

All model inference runs locally via Ollama; the running agent makes no cloud
LLM calls and never reads or writes outside its configured project sandbox.
"""

__version__ = "0.1.0"
